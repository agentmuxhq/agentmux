# CodeQL workflow - DISABLED
# To re-enable, restore the original triggers and uncomment the jobs section

name: CodeQL (DISABLED)

# DISABLED - only manual trigger to prevent notification spam
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled'
        required: false
        default: 'Workflow disabled - jobs commented out'

env:
  NODE_VERSION: 22
  GO_VERSION: "1.24"

# DISABLED - jobs section commented out
# Original triggers were:
#   push:
#     branches: ["main"]
#     paths: ["**/*.go", "**/*.ts", "**/*.tsx"]
#   pull_request:
#     branches: ["main"]
#     paths: ["**/*.go", "**/*.ts", "**/*.tsx"]
#   schedule:
#     - cron: "36 5 * * 5"
