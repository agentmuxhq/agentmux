# Docsite and Storybook CI/CD workflow - DISABLED
# To re-enable, restore the original triggers and uncomment the jobs section

name: Docsite and Storybook CI/CD (DISABLED)

# DISABLED - only manual trigger to prevent notification spam
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled'
        required: false
        default: 'Workflow disabled - jobs commented out'

env:
  NODE_VERSION: 22

# DISABLED - jobs section commented out
# Original triggers were:
#   push:
#     branches: [main]
#   workflow_dispatch:
#   pull_request:
#     branches: [main]
#     paths: ["docs/**", "storybook/**", "**/*.story.*", "**/*.stories.*", ".github/workflows/deploy-docsite.yml", "Taskfile.yml"]
