# TestDriver.ai Build workflow - DISABLED
# To re-enable, restore the original triggers and uncomment the jobs section

name: TestDriver.ai Build (DISABLED)

# DISABLED - only manual trigger to prevent notification spam
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled'
        required: false
        default: 'Workflow disabled - jobs commented out'

env:
  GO_VERSION: "1.24"
  NODE_VERSION: 22

permissions:
  contents: read
  pull-requests: write

# DISABLED - jobs section commented out
# Original triggers were:
#   push:
#     branches: [main]
#     tags: ["v[0-9]+.[0-9]+.[0-9]+*"]
#   pull_request:
#     types: [opened, synchronize, reopened, ready_for_review]
#   schedule:
#     - cron: 0 21 * * *
#   workflow_dispatch:
